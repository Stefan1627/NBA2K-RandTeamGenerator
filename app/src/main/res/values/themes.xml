<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.NBA_Team_Rand_Gen" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:elevation">10dp</item>
        <item name="android:windowBackground">@color/splash_color</item>
    </style>

    <style name="Theme.NBA_Team_Rand_Gen.AlertDialog" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <!-- override dialog attributes here -->
        <item name="android:windowBackground">@color/splash_color</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
        <item name="colorSecondary">@android:color/white</item>
    </style>
</resources>